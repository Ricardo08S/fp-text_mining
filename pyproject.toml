[project]
name = "pubmed-topic-xai-app"
version = "0.1.0"
description = "Read-only FastAPI dashboard for PubMed topic XAI scenario 3 outputs."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0",
    "uvicorn[standard]>=0.35.0",
]

[tool.uvicorn]
factory = false
